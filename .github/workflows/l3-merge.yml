name: "L3 - Deploy"

on:
  push:
    branches: [ main ]

jobs:
  call-l3:
    uses: haimazulay/github-k8s-cicd-templates/.github/workflows/l3-merge-deploy.yml@main
    with:
      helm_chart_dir: ./helm/app-chart
      k8s_namespace: k8s-cicd-sample-service
      helm_release_name: k8s-cicd-sample-service
    secrets: inherit
